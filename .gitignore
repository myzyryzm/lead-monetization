# Python
__pycache__/
*.py[cod]
backend/.venv/
.venv/

# Secrets — rotate the committed key and keep this out of git
.env
backend/.env

# Persisted model weights (regenerated on first run)
backend/model.pkl

# Campaign ledger (SQLite + WAL siblings; recreated on first stage)
backend/campaigns.db*

# Frontend
frontend/node_modules/
frontend/dist/