# Python
__pycache__/
*.py[cod]
backend/.venv/
.venv/

# Secrets — rotate the committed key and keep this out of git
.env
backend/.env

# Persisted model weights (regenerated on first run)
backend/model.pkl

# Flywheel simulation outputs (regenerated by `python flywheel.py`)
backend/flywheel_report.html
backend/flywheel_results.csv

# Frontend
frontend/node_modules/
frontend/dist/
