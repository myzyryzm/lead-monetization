# Core data + ML
pandas>=2.0
numpy>=1.24
scikit-learn==1.9.0   # pinned: model.pkl must unpickle under the same version
joblib>=1.3           # model persistence (also a scikit-learn dependency)

# LLM layer (copy generation) + agent orchestration
anthropic>=0.40

# Flask API + serving the React build
flask>=3.0
flask-cors>=4.0
python-dotenv>=1.0    # load ANTHROPIC_API_KEY from backend/.env
